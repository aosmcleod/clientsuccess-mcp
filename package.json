{
  "name": "clientsuccess-mcp",
  "version": "2.0.4",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:mcpb": "npm run build && node build.js",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "setup": "npm install"
  },
  "devDependencies": {
    "archiver": "^6.0.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  }
}
